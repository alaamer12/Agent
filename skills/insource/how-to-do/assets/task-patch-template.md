##### Patch — <YYYY-MM-DD>

**Raw query:** "<the exact request that surfaced this gap>"
**Interpolated query:** <what the agent understood this request to concretely mean>

**Missing/extra step found:**
> <one or two sentences describing what the existing steps didn't cover, and what actually needed to happen instead>
- Files:
  - `<full path>` — <exact edit>

**Insert as:** <e.g. "new Step 4.5" | "amendment to Step 5's Files list" | "new optional step with condition: ...">

**Scope note:** <does this patch apply to every future instance of this task-ID, or only to a narrower case within it? Say so explicitly — a patch found for one variant (e.g. "soft-delete columns") should not silently generalize to the whole task-ID until that's actually confirmed across repeated instances.>
