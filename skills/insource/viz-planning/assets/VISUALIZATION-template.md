# VISUALIZATION.md — [Project name]

Generated by the `viz-planning` skill. This is the build spec — every checkbox is one decision made during planning. Whoever implements the charts should be able to work from this file alone, without re-reading the planning conversation. Check items off as they're implemented.

One decision per line. Don't expand a line into a paragraph — if a decision needs real explanation, add a short `Note:` sub-line directly under it, but the checkbox line itself stays a single line.

Numbering: `G` = global (applies to the whole project), then one letter block per chart (`A`, `B`, `C`...). Renumber if items are added/removed — the numbers just need to be unique and stable while this doc is in use.

---

## Data understanding
_(copied from Step 2's findings report — keep this so decisions below are traceable to evidence)_

- Source: [file/URL/API/DB — path or description]
- Volume: [N records]
- Schema: [stable / drifts — describe]
- Quality notes: [nulls, duplicates, empty fields found]
- Ordering: [inherently ordered by field X / no inherent order detected]

## Global decisions
- [ ] G1. Unknown-field policy: [ignore / display / warn / reject]
- [ ] G2. Null/empty handling: [treat as — per field if it varies]
- [ ] G3. Filters represented as: [URL query params / local component state / none]
- [ ] G4. Persisted state: [what lives in the URL vs. local storage vs. nothing]
- [ ] G5. Config precedence (if more than one config source exists): [order]
- [ ] G6. Input normalization: [how multiple formats/sources get unified, if applicable]
- [ ] G7. Caching/refresh strategy: [static load / poll interval / delta-loading]
- [ ] G8. Color semantics used dashboard-wide: [meaning → color mapping]
- [ ] G9. Customization scope: [same treatment across all charts / per-chart, see below]
- [ ] G10. Audience literacy: [general/non-technical / data-literate / mixed] — drives which chart families are viable
- [ ] G11. Chart count justified: [each chart below answers a distinct question — none were mergeable into an existing chart, facet, or table row]
- [ ] G12. Number precision policy: [uniform dashboard-wide / per-field — list exceptions, e.g. "currency: 2dp, percentages: 1dp, counts: 0dp"]

## Chart A — [name / what it answers]
- [ ] A1. Chart type: [bar / line / scatter / table / box plot / etc.]
- [ ] A2. Field(s) used: [field names]
- [ ] A3. Grouping: [none / bucket field X into Y-wide bins]
- [ ] A4. Aggregation: [none / sum / avg / count / median — per group if grouped]
- [ ] A5. "Others" handling: [none / fold categories below N% into "Others"]
- [ ] A5b. Time window shown (if time-based): [full history / last N days-weeks-months / full span at coarser rollup] + how full detail stays reachable [zoom / date picker / "view all" link / n/a]
- [ ] A6. Outlier detection: [none / method — e.g. IQR, Z-score]
- [ ] A7. Outlier representation: [color/shape if shown]
- [ ] A8. Outliers in calculations: [included / excluded / user-toggle]
- [ ] A9. Focus entity: [none / entity name + how it's highlighted]
- [ ] A10. Legend: [none / position + interactive or static]
- [ ] A11. Tooltip contents: [fields shown on hover]
- [ ] A12. Interactivity: [none / click-to-filter / drill-down / zoom-pan]
- [ ] A13. Custom transform/algorithm (if any): [name it explicitly — e.g. "strikes computed via <algorithm>"]

## Chart B — [name / what it answers]
- [ ] B1. Chart type:
- [ ] B2. Field(s) used:
- [ ] B3. Grouping:
- [ ] B4. Aggregation:
- [ ] B5. "Others" handling:
- [ ] B5b. Time window shown (if time-based):
- [ ] B6. Outlier detection:
- [ ] B7. Outlier representation:
- [ ] B8. Outliers in calculations:
- [ ] B9. Focus entity:
- [ ] B10. Legend:
- [ ] B11. Tooltip contents:
- [ ] B12. Interactivity:
- [ ] B13. Custom transform/algorithm (if any):

_(repeat the per-chart block for each additional chart — delete unused rows within a block rather than leaving them blank; only include rows that are actually decisions for that chart)_

## Layout
- [ ] L1. Dashboard grid: [N columns, fixed/responsive]
- [ ] L2. Chart reordering/resizing: [allowed / fixed layout]
- [ ] L3. Long-text strategy: [truncate+tooltip / wrap / rotate — per label type if it varies]

## Explicitly out of scope for this version
- [ ] Note anything triaged as "not applicable" that's worth naming so it isn't silently forgotten later, e.g. "no delta-loading yet — dataset is small enough to reload fully for now."
